import { Box } from "@chakra-ui/react";
import { Text } from "@/components/Information/Text";

export const InitialDescription = () => {
  return (
    <Box>
      <Text>
        Kenshiro
        Kasamoto（以下，「当方」）が提供するサービス（以下，「本サービス」）における，利用者情報の取扱いについて，以下のとおりプライバシーポリシー（以下，「本ポリシー」といいます。）を定めます。
      </Text>
    </Box>
  );
};
